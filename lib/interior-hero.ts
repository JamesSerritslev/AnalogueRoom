/**
 * Fallback interior path when Sanity `siteImagery` is empty (see `getSiteImagery()`).
 * Prefer resolving URLs from Studio via `getSiteImagery()` in app code.
 */
export const INTERIOR_HERO_SRC = "/images/interior.jpeg"
